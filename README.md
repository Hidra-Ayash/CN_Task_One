# CN_Task_One
this repo for the first task of our project CN using python 
🚀 خطة عمل مشروع أتمتة DHCP بواجهة بايثون (GUI) و GNS3
تتكون الخطة من خمس مراحل رئيسية، تبدأ بالإعداد وتنتهي بالاختبار والتسليم.
المرحلة الأولى: الإعداد والتجهيز الأساسي
هذه المرحلة تركز على تجهيز بيئة العمل.
•	تثبيت وتجهيز الأدوات:
o	تثبيت Python (يفضل أحدث إصدار) وتجهيز بيئة افتراضية (Virtual Environment).
o	تثبيت مكتبات تطوير الواجهات الرسومية لـ Python (مثل Tkinter أو PyQt، يوصى بـ Tkinter للمشاريع البسيطة أو PyQt/PySide للمشاريع الأكثر احترافية).
o	تثبيت محاكي GNS3 ودمج جهاز المحاكاة الافتراضية (Virtual Machine) الخاص به (GNS3 VM).
o	تثبيت محرر أكواد مناسب (مثل VS Code).
•	تصميم هيكل المشروع:
o	وضع مخطط بسيط للواجهة الرسومية (Wireframe) لتحديد أماكن الأزرار والحقول (مثل حقول إدخال نطاق IP، Gateways، Subnet Mask، وما إلى ذلك).
o	تحديد مكونات الشبكة الأساسية التي سيتم استخدامها في GNS3 (مثل راوتر، سويتش، وHosts/PCs).
________________________________________
المرحلة الثانية: تطوير الواجهة الرسومية (GUI) باستخدام بايثون
هنا يتم بناء واجهة التفاعل مع المستخدم.
•	بناء الهيكل الأساسي للواجهة:
o	إنشاء نافذة التطبيق الرئيسية وتخطيط العناصر (Layout).
o	إضافة الحقول النصية (Entry Fields) لإدخال معلمات DHCP الأساسية:
	Start IP Address (بداية النطاق)
	End IP Address (نهاية النطاق)
	Subnet Mask
	Default Gateway
	DNS Servers (أساسي وثانوي)
o	إضافة زر "Apply/Configure DHCP" وزر "Show Status/Logs".
•	إنشاء الوظائف (Functions):
o	كتابة دالة (Function) لاستخراج البيانات المدخلة من الواجهة.
o	كتابة دالة لتوليد ملف تكوين DHCP (مثل ملف dhcpd.conf إذا كنت ستستخدم خادم DHCP يعمل بنظام Linux داخل GNS3، أو أوامر Cisco IOS/Juniper JunOS).
o	تضمين آليات للتحقق من صحة الإدخال (Input Validation) مثل التحقق من تنسيق IP الصحيح.
________________________________________
المرحلة الثالثة: أتمتة تكوين الشبكة (Network Automation)
هذه هي المرحلة الجوهرية لربط بايثون بالشبكة.
•	اختيار أداة الأتمتة:
o	استخدام مكتبة Netmiko أو Paramiko في بايثون لإنشاء اتصال SSH أو Telnet بجهاز الشبكة (الراوتر/السيرفر) داخل GNS3.
o	بديل: إذا كنت تستخدم خادم Linux كخادم DHCP، استخدم Paramiko لتنفيذ الأوامر عن بعد لتعديل ملف التكوين (مثل /etc/dhcp/dhcpd.conf) وإعادة تشغيل الخدمة.
•	برمجة سكريبت الأتمتة:
o	كتابة كود بايثون يقوم بـ:
1.	إنشاء اتصال SSH بجهاز الشبكة في GNS3.
2.	أخذ البيانات المدخلة من الواجهة الرسومية (المرحلة الثانية).
3.	تحويل هذه البيانات إلى أوامر تكوين قياسية لـ DHCP (مثل أوامر الـ Router/Server).
4.	إرسال الأوامر بالترتيب عبر اتصال SSH للجهاز.
5.	التقاط مخرجات الأوامر لعرضها في الواجهة (للتأكد من نجاح التكوين).
مثال على أوامر Cisco DHCP:
config terminal
ip dhcp pool [Pool_Name]
network [Network_ID] [Subnet_Mask]
default-router [Gateway_IP]
•	تكامل الواجهة والسكربت:
o	ربط زر "Apply/Configure DHCP" بالدالة التي تنفذ سكريبت الأتمتة بالكامل.
________________________________________
المرحلة الرابعة: بناء شبكة GNS3 والربط
الآن يجب إعداد البيئة الافتراضية لتلقي الأوامر.
•	تصميم طوبولوجيا GNS3:
o	إنشاء طوبولوجيا بسيطة: [Router/Switch (DHCP Server)] $\leftarrow$ [Switch] $\leftarrow$ [VPCS/Host].
o	تكوين العناوين الأساسية للمنافذ (IP addresses on interfaces) لضمان قابلية الوصول (Reachability) بين جهاز الأتمتة والراوتر/السيرفر.
•	تفعيل الاتصال البعيد:
o	تأكد من أن جهاز الشبكة (الراوتر/السيرفر) في GNS3 لديه خدمة SSH أو Telnet مفعلة وجاهزة لاستقبال الاتصالات.
o	تحتاج إلى ربط بيئة بايثون (جهازك الفعلي) بشبكة GNS3 باستخدام جهاز Cloud/Host في GNS3 أو باستخدام طريقة مناسبة لـ GNS3 VM. هذا يسمح لسكربت بايثون بالاتصال بالـ IP الخاص بالجهاز داخل المحاكي.
•	اختبار الاتصال:
o	اختبار الاتصال بـ SSH من جهازك إلى جهاز GNS3 يدوياً أولاً للتأكد من أن الاتصال يعمل قبل تشغيل سكريبت بايثون.
________________________________________
المرحلة الخامسة: الاختبار والتوثيق والتسليم
الخطوات النهائية لإنهاء المشروع.
•	الاختبار الوظيفي (Functional Testing):
o	إدخال نطاقات DHCP مختلفة في الواجهة والضغط على "Apply".
o	التحقق من جهاز Host/PC (VPCS) داخل GNS3 للتأكد من أنه يتلقى عنوان IP و Gateway و DNS بشكل صحيح من النطاق المحدد.
o	اختبار حالات الخطأ (مثل إدخال IP خاطئ) والتحقق من أن الواجهة تعالجها بشكل جيد.
•	التوثيق التقني:
o	إعداد تقرير تفصيلي يشمل:
	شرحاً للتقنيات والمكتبات المستخدمة (Tkinter/PyQt، Netmiko/Paramiko).
	شرحاً لآلية عمل الكود وكيفية تحويل مدخلات الـ GUI إلى أوامر شبكة.
	شرحاً لخطوات بناء طوبولوجيا GNS3 وإعداد الاتصال.
•	العرض والتسليم:
o	الاستعداد لعرض المشروع أمام المشرف، وتجهيز سيناريوهات العرض التي توضح سهولة استخدام الواجهة وقوة الأتمتة.

